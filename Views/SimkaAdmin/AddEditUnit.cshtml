@model GabungUnit
@{
    Layout = null;
}
<h4>Todo</h4>
<hr />
<div class="row">
    <div class="col-md-12">
        <form onsubmit="return SubmitAddEdit (this)">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdUnit" />
            Kepala Unit:
            <div class="form-group">
                <select asp-for="Npp"
                        asp-items="@(new SelectList(Model.kepala,"Npp","Nama"))">
                </select>
          
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
</div>
</div>